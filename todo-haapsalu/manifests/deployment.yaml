apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo-haapsalu
  namespace: haapsalu
spec:
  replicas: 1
  selector:
    matchLabels:
      app: todo
  template:
    metadata:
      labels:
        app: todo
    spec:
      containers:
        - name: todo
          env:
            - name: COMPANY_NAME
              valueFrom:
                configMapKeyRef:
                  name: todo-config
                  key: COMPANY_NAME
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: todo-config
                  key: PORT
          ports:
            - containerPort: 8081
      imagePullSecrets:
        - name: ghcr-secret
